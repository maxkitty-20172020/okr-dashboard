# 运营部 · 任务与进展

用于业务、团队和库存管理的部门事项看板。成员记录任务及协作交付，老板查看部门全貌。

## 启动与升级

```bash
npm install
cp .env.example .env  # 已有配置请保留，不要覆盖
touch prisma/dev.db  # 默认 SQLite 配置；全新安装时创建空文件，已有内容不会覆盖
npx prisma migrate deploy
npx prisma generate
npm run dev
```

浏览器打开 [http://localhost:3000](http://localhost:3000)。保留原来的 `AUTH_SECRET` 和 `DATABASE_URL`；新安装时将 `AUTH_SECRET` 改为随机字符串。

已有数据升级使用 `prisma migrate deploy`，不要使用数据库重置命令。迁移保留任务 ID、原负责人、原所属周、状态和目标关联；旧任务的完成时间沿用原记录更新时间，并在历史记录中说明。不会给旧任务自动生成虚假的进度确认。

SQLite 默认位于 `prisma/dev.db`。升级前应使用 SQLite backup 方式备份，运行期间不要仅复制主文件而遗漏 WAL。代码提交到 Git 不会同步数据库。

## 周期、WBS、RACI 与 Check-in

本版先落地数据模型，供后续 OKR + WBS 排期、Check-in 和老板下钻使用。

- **Period**：周 / 月 / 季树（Week→Month→Quarter）。季度标签与原 `cycle` 字符串一致（如 `2026-Q3`），月份为 `2026-09`，周为 ISO 周标签（如 `2026-W37`），日历与 `lib/week.ts` 对齐，业务时区仍为 Asia/Shanghai。`Objective.cycle` 保留兼容，同时回填 `periodId`。
- **WBS**：`WorkPackage` / `Deliverable` 可挂到月或周；跨周期任务只改 `schedulePeriodId` / `dueAt`，不会因此归档或删除。
- **RACI**：与账号读写角色（`User.role` 的 BOSS / MANAGER）分开。v1 每个对象最多一名 A、一名 R；C / I 可多人。存量任务回填为负责人同时担任 R 和 A，有效协作者为 C。
- **Check-in**：按周期 + 范围汇报。没有对应行即为「未汇报」。`SUBMITTED` 为提交内容，`NO_CHANGE` 为确认暂无变化。任务进展审计仍用 `TaskUpdate`。

例行任务可以不挂 `keyResultId`：计入任务完成率，不计入 KR 达成率。两种比率分开统计。

`TaskCollaborator` / `TaskMilestone` 这一版保留；协作者交付文案会复制一份到 `Deliverable`，原协作行不删除。

## 初次体验

仅空数据库可以初始化演示数据：

```bash
npm run db:seed
```

- 老板：`okr`（陈总），只读。
- 填报成员：`okr1`（李明）、`okr2`（王芳）、`okr3`（赵强）。
- 演示密码：`okr12345`，支持短账号或完整邮箱登录。

`seed` 检测到已有账号会直接退出，不会重置任务或改名。页面按实际账号动态显示，没有三人或四人上限。账号邀请和成员管理页面尚未包含在这一版。

## 第一版工作流程

1. 创建任务：填写责任领域、主负责人、完成标准、下一步动作和跟进日期；完成期限可暂时留空。
2. 安排协作：主负责人指定协作者、交付内容和期限。协作者只能更新自己的交付。
3. 持续跟进：主负责人记录整体进展，或选择「确认暂无变化」。暂无变化只记录确认并调整跟进日期，不覆盖已有进展事实。
4. 长期事项按关键节点推进，节点或个人交付完成都不会自动把整体任务标为完成。
5. 完成或取消后可以归档，也可以恢复或重新开启，历史记录持续保留。

首页及任务清单按所有未结束事项查询，不按自然周隔断任务；旧 `/week` 链接会转到新的任务清单。周完成数仅统计完成时间位于本周的任务。日期以 Asia/Shanghai 为业务时区。

风险、整体逾期和跟进确认到期分别显示。刚创建或移交的任务显示「情况待确认」，不会自动宣称按计划。

## 权限与保存

- 老板账号：读取总览、任务详情及目标，所有业务写入在服务端禁止。
- 填报成员：可创建任务、维护资料和调整分工；主负责人更新整体进展、安排协作和关键节点；协作者更新自己的交付。
- 移交会清除当前确认标记，由新负责人重新确认。原老板名下的历史任务保留，由填报成员移交接手。
- 每次更新与历史记录在同一事务保存。表单携带版本号，旧页面提交会提示刷新，不会覆盖别人刚保存的修改。
- 任务采用归档保留机制；原有目标编辑功能仍保留。

目标支持「提升」和「降低」两种方向及起点值。例如 ACOS 起点 35、目标 25、当前 30 的进度为 50%。不能将不同指标的百分比简单当作部门总体表现。

本版包含页面内到期跟进提示；定时外部通知、周期任务自动生成和独立复盘快照安排在后续版本。当前为本地应用，多人异地使用需部署到同一服务并做好数据库备份，尚未配置公网部署。

## 验证

```bash
npm test
npm run lint
npm run build
```

测试使用独立临时 SQLite 数据库，覆盖迁移保留、跨周任务、多人协作、老板写入拦截、跟进确认、并发版本冲突、日期校验、归档恢复、降低类指标，以及 Period 树、目标周期回填、RACI 默认值、Check-in 未汇报和任务完成率 / KR 达成率。
